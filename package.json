{
  "name": "grwire-next",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/index.js && node --check scripts/scan.js",
    "scan": "node scripts/scan.js"
  },
  "engines": { "node": ">=20" }
}
